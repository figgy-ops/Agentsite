{
  "name": "agentsite",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check api/main.js && node --check api/_lib/security.js && node --check api/_lib/discovery.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  }
}
